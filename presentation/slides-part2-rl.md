---
title: "Part II — Reinforcement learning extension"
theme: default
class: text-left
highlighter: shiki
transition: fade
mdc: true
---

# 2.1. Part II — Reinforcement learning extension

## From implicit OC to learned policies with unknown dynamics

- We keep the **value-function parameterization** $\phi_\theta(t,z)$.
- The control is still defined implicitly through the Hamiltonian stationarity condition.
- New difficulty: the dynamics $f$ are **unknown**.
- We only observe transitions from rollouts.

$$
\text{Unknown: } f,\ \nabla_z f,\ \nabla_u f
\qquad
\text{Known: } L,\ G,\ \rho
$$

---

## 2.2. From continuous OC to discrete rollouts

Continuous-time dynamics:

$$
\dot z(t)=f(t,z(t),u(t)), \qquad z(0)=x.
$$

Discretize on $t_k=k\Delta t$, $k=0,\dots,N$, $T=N\Delta t$:

$$
z_{k+1}=F(t_k,z_k,u_k),
$$

with the one-step transition map

$$
F(t,z,u) =
z+\Delta t\, f(t,z,u)+O(\Delta t^2).
$$

The empirical RL setting gives access to samples

$$
(z_k,u_k,z_{k+1}),
$$

but not to the analytical formula of $F$.

---

## 2.3. Discrete objective

For one initial condition $x$, the discretized objective is

$$
J_x(\theta) =
\sum_{k=0}^{N-1}
L(t_k,z_k,u^\star_{\theta,k})\,\Delta t
+
G(z_N).
$$

The training objective is therefore

$$
\min_\theta \ \mathbb E_{x\sim \rho} J_x(\theta).
$$

- Same optimal-control objective as before.
- But the trajectory is now generated by interacting with the environment.

---

## 2.4. Local Jacobian estimation: target

At each training iteration, we roll out the current implicit policy and obtain

$$
\bar z_{0:N},
\qquad
\bar u_{0:N-1}.
$$

We estimate local Jacobians of the unknown transition map:

$$
A_k \approx \nabla_z F(t_k,\bar z_k,\bar u_k),
\qquad
B_k \approx \nabla_u F(t_k,\bar z_k,\bar u_k).
$$

These replace the missing analytical derivatives of the dynamics.

---

## 2.5. Off-trajectory / RLS Jacobian estimation
**First-order Taylor expansion of the unknown dynamics** around a reference $(\bar z_t, \bar u_t)$:

$$
z_{t+1} \approx A_t z_t + B_t u_t + c_t = F_t\, x_t,
\qquad
F_t = [\,A_t \ \ B_t \ \ c_t\,],
\qquad
x_t = \begin{bmatrix} z_t \\ u_t \\ 1 \end{bmatrix},
$$

with $A_t = \nabla_z f_t$, $B_t = \nabla_u f_t$, and the "1" absorbing the affine offset $c_t$. RLS thus estimates the local **Jacobians of the dynamics** directly from rollouts:

$$
Q_t^{(k)} =
\alpha_{\mathrm{rls}} Q_t^{(k-1)}
+ (1-\alpha_{\mathrm{rls}})q_0 I
+ x_t^{(k)}(x_t^{(k)})^\top,
$$

$$
F_t^{(k)} = F_t^{(k-1)} + \left(Q_t^{(k)}\right)^{-1} x_t^{(k)} \left(z_{t+1}^{(k)} - F_t^{(k-1)} x_t^{(k)}\right)^\top.
$$

- $t$ = time index inside a trajectory, $k$ = rollout index.
- Works best when successive rollouts remain locally informative.
---

## 2.6. Local linear transition model

Both estimation methods produce a local model

$$
\widehat F_k(z,u) =
\bar z_{k+1}
+
A_k^\top(z-\bar z_k)
+
B_k^\top(u-\bar u_k).
$$

It is valid only near the current trajectory point

$$
(\bar z_k,\bar u_k).
$$

This is the key replacement:

$$
\text{unknown } F
\quad \leadsto \quad
\text{local estimated linearization } \widehat F_k.
$$

---

## 2.7. Estimated Hamiltonian

Define the estimated continuous-time vector field

$$
\widehat f_k(z,u) =
A_k^\top(z-\bar z_k)
+
B_k^\top(u-\bar u_k)
+
\frac{\bar z_{k+1}-\bar z_k}{\Delta t}.
$$

Then the estimated Hamiltonian is

$$
\widehat H_k(z,p,u) = -\langle p,\widehat f_k(z,u)\rangle -
L(t_k,z,u).
$$

The fixed-point operator becomes

$$
\widehat T_{\theta,k}(u;z) =
u+\alpha
\nabla_u \widehat H_k
\bigl(
z,\nabla_z\phi_\theta(t_k,z),u
\bigr).
$$

---

## 2.8. Key simplification

The control-gradient of the estimated Hamiltonian is

$$
\nabla_u \widehat H_k(z,p,u) = -B_k^\top p -
\nabla_u L(t_k,z,u).
$$

Therefore the implicit policy can be computed without evaluating $f$:

$$
u^\star_{\theta,k} =
\widehat T_{\theta,k}
(
u^\star_{\theta,k};z_k
).
$$

The only dynamics information needed inside the fixed-point operator is

$$
B_k \approx \nabla_u F.
$$

---

## 2.9. JFB gradient with estimated dynamics

The exact gradient has the structure

$$
\frac{dJ_x}{d\theta} =
\int_0^T
\left(
\frac{d u^\star_\theta}{d\theta}(t,\bar z_t)
\right)^\top
\left[
\nabla_u L(t,\bar z_t,u^\star_\theta)
+
(\nabla_u f)^\top p_t
\right]dt.
$$

But both terms are problematic:

$$
\nabla_u f \text{ unknown},
\qquad
\frac{d u^\star_\theta}{d\theta} =
\left(
I-\frac{\partial T_\theta}{\partial u}
\right)^{-1}
\frac{\partial T_\theta}{\partial\theta}
\text{ expensive}.
$$

---

## 2.10. Gradient actually used

We replace

$$
\nabla_u f
\quad \leadsto \quad
B_k,
$$

and apply JFB:

$$
\frac{d u^\star_\theta}{d\theta}
\quad \leadsto \quad
\frac{\partial \widehat T_{\theta,k}}{\partial\theta}.
$$

Therefore the estimated JFB descent direction is

$$
\widehat{\frac{dJ_x}{d\theta}} =
\sum_{k=0}^{N-1}
\left(
\frac{\partial \widehat T_{\theta,k}}{\partial\theta}
\right)^\top
\left[
\nabla_u L(t_k,\bar z_k,u^\star_{\theta,k})
+
B_k^\top \widehat p_{k+1}
\right]
\Delta t.
$$

This is the central training formula of the RL extension.

---

## 2.11. Algorithmic summary

<pre v-pre class="algo-box"><code><span class="algo-line-muted"><span class="algo-ln"> 1:</span>  <span class="algo-kw">Initialize</span> networks with parameters <span class="algo-math">θ</span>, RLS estimates <span class="algo-math">{F_k, Q_k}</span></span>
<span class="algo-line-muted"><span class="algo-ln"> 2:</span>  <span class="algo-kw">for</span> iteration = 1, 2, … <span class="algo-kw">do</span></span>
<span class="algo-line-muted"><span class="algo-ln"> 3:</span>      Sample a batch of initial states <span class="algo-math">{x_i} ∼ ρ</span></span>
<span class="algo-line-muted"><span class="algo-ln"> 4:</span>      <span class="algo-kw">for</span> each trajectory <span class="algo-kw">do</span></span>
<span class="algo-ln"> 5:</span>         <span class="algo-kw">for</span> <span class="algo-math">k</span> = 0, …, <span class="algo-math">N_t − 1</span> <span class="algo-kw">do</span>
<span class="algo-ln"> 6:</span>              INN solves fixed point eq for optimal <span class="algo-math">u_k = T̂_{θ,k}(u_k; z_k)</span>  <span class="algo-cm"># K detached + K′ on-graph</span>
<span class="algo-ln"> 7:</span>              Rollout in the environment <span class="algo-math">z_{k+1} = F(t_k, z_k, u_k)</span>
<span class="algo-ln"> 8:</span>              RLS update of local Jacobians <span class="algo-math">A_k, B_k</span> from <span class="algo-math">(z_k, u_k, z_{k+1})</span>
<span class="algo-ln"> 9:</span>         <span class="algo-kw">end for</span>
<span class="algo-ln">10:</span>         Backward adjoint pass <span class="algo-math">p_k = A_k^⊤ p_{k+1} + Δt ∇_z L</span>
<span class="algo-line-muted"><span class="algo-ln">11:</span>      <span class="algo-kw">end for</span></span>
<span class="algo-line-muted"><span class="algo-ln">12:</span>      JFB update <span class="algo-math">θ ← θ − η · d̂J_x/dθ</span></span>
<span class="algo-line-muted"><span class="algo-ln">13:</span>  <span class="algo-kw">end for</span></span>
</code></pre>

---

## 2.12. Van der Pol oscillator — control problem

**Goal.** Learn a feedback control that stabilizes a nonlinear oscillator and drives the state to $z_{\text{target}} = (0,0)$.

**State.** $z(t) = (x_1, x_2)^\top$, with $x_1$ position-like, $x_2$ velocity-like.

**Controlled dynamics.**

$$
\dot x_1 = x_2,
\qquad
\dot x_2 = \underbrace{\mu(1-x_1^2)x_2 - x_1}_{\text{natural nonlinear dynamics}} + \underbrace{u}_{\text{external force}}.
$$

**Control.** $u(t)\in\mathbb R$ — a scalar input chosen at each time step to counteract the oscillations and steer the system to the origin.

---

## 2.13. Portfolio optimal control — control problem

**Goal.** Learn an investment policy that allocates wealth $W_t \in \mathbb R$ to a risky asset, balancing **wealth growth** against a **risk/concentration penalty**.

**Dynamics.**

$$
\dot W = rW + \pi(\mu - r)W,
$$

with $\mu$ = risky-asset drift, $r$ = risk-free rate — **both treated as unknown** in the RL setting.

**Objective.** Minimize

$$
\int_0^T L(t,W,\pi)\,dt + G(W_T),
\qquad
L = \lambda\!\left(e^{\pi^2}-1\right),
\qquad
G(W) = -\log\!\left(\tfrac{W}{W_{\mathrm{ref}}}\right).
$$

The agent seeks high terminal wealth while penalizing aggressive positions.

---

## 2.14. Van der Pol — learned control vs Oracle (RL)

Runs from `examples-RL/vanderpol_comparison.py` / `VanDerPolOC_RL`  
oracle vs JFB–RL / RLS-style training.

<img src="/images/rl/vanderpol_oracle_vs_rls.png" alt="Van der Pol: oracle vs RL comparison" class="mx-auto block max-h-80 object-contain" />

---

## 2.15. Portfolio OC — RL training (JFB–RL / RLS)

Artifacts from `examples-RL/portfolio_optimization_RL.py` and `PortfolioOC_RL` results  
same run tag: `JFB-RL_RLS_20260515_180609`.

<div class="grid grid-cols-2 gap-4 items-start">

<img src="/images/rl/portfolio_loss_curve.png" alt="Portfolio RL training loss" class="w-full max-h-56 object-contain mx-auto" />

<img src="/images/rl/portfolio_policy_rollout.png" alt="Portfolio learned policy rollout" class="w-full max-h-56 object-contain mx-auto" />

</div>

**Mid-training rollout** epoch step `0200`:

<img src="/images/rl/portfolio_rollout_training_epoch200.png" alt="Portfolio rollout during training" class="mx-auto block max-h-48 object-contain" />

---

## 2.16. Practical challenges

- **Locality of RLS:** old rollouts may be misleading if they are far from the current trajectory.
- **Jacobian warm-up:** early $A_k,B_k$ estimates may be noisy.
- **Contractivity:** the fixed-point operator must remain stable.
- **Long-horizon sensitivity:** errors in $A_k$ propagate backward through the adjoint recursion.
- **Exploration:** enough perturbation is needed to identify $B_k$.

Diagnostics:

$$
\|z_{k+1}-\widehat F_k(z_k,u_k)\|,
$$

$$
\partial_t\phi_\theta+\sup_u \widehat H_k,
$$

and fixed-point residuals.

---

## 2.17. Outlook

The extension keeps the Gelphman structure:

$$
\phi_\theta
\quad\rightarrow\quad
\nabla_z\phi_\theta
\quad\rightarrow\quad
u^\star_\theta
\quad\rightarrow\quad
J(\theta).
$$

But replaces analytical dynamics by estimated local Jacobians:

$$
f,\nabla f
\quad\leadsto\quad
A_k,B_k.
$$

Next steps:

- unknown $L$ via cost-gradient estimation;
- stochastic dynamics and second-order HJB terms;
- stronger proof of descent under simultaneous JFB and Jacobian-estimation error;
- multi-asset portfolio benchmarks.